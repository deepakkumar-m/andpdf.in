services:
  - type: web
    name: pdf-utilities
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8000
      - key: ENV
        value: production
    autoDeploy: true
    # Custom domain configuration (optional)
    domains:
      - name: andpdf-in.onrender.com
        verify: true